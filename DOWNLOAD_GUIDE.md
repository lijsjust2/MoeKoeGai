# 批量下载功能使用说明

## 访问方式
直接在浏览器中访问：http://localhost:8880/#/download

## 功能说明

### 1. 输入歌手ID
- 在"歌手ID"输入框中输入酷狗音乐的歌手ID
- 例如：周杰伦的歌手ID可能是某个数字

### 2. 排除选项
- **排除演唱会**：自动过滤掉标题或专辑名中包含"演唱会"的歌曲
- **排除Live版本**：自动过滤掉标题或专辑名中包含"live"的歌曲
- 两个选项默认都是选中的

### 3. 音质选择（可多选）
- **128K**：标准音质，文件较小
- **320K**：高品质音质
- **FLAC**：无损音质，文件较大
- 可以同时选择多个音质，系统会分别下载每种音质

### 4. 下载间隔时间
- 设置每首歌下载之间的随机等待时间（秒）
- 例如：输入 3-5 秒，则每首歌下载后会随机等待3到5秒
- 这样可以避免请求过于频繁被封禁
- 留空则无延迟

### 5. 使用流程

#### 第一步：获取歌曲列表
1. 输入歌手ID
2. 配置好排除选项、音质选择和延迟时间
3. 点击"开始获取"按钮
4. 系统会自动：
   - 获取该歌手的所有专辑
   - 获取每个专辑中的所有歌曲
   - 去重（同一首歌只保留一次）
   - 应用排除条件过滤
   - 显示最终的歌曲列表

#### 第二步：开始下载
1. 确认歌曲列表无误
2. 点击"开始下载"按钮
3. 系统会：
   - 逐个检查每首歌的可用音质
   - 下载你选择的每种音质
   - 在下载之间按照设置的延迟时间等待
   - 实时显示下载进度和日志

### 6. 下载结果
- 下载的文件会自动保存到浏览器的默认下载文件夹
- 文件命名格式：`歌曲名_音质.扩展名`
  - 例如：`晴天_128.mp3`、`晴天_320.mp3`、`晴天_flac.flac`
- 下载完成后会弹出提示

### 7. 注意事项
- 下载大量歌曲时请耐心等待，不要关闭页面
- 可以通过日志区域查看详细的下载过程
- 如果某些歌曲无法下载（版权等原因），系统会自动跳过并继续下一首
- 建议设置合理的下载间隔（3-5秒），避免请求过于频繁
- 部分歌曲可能没有所有音质可选，系统会自动检测可用音质

## 如何获取歌手ID？
1. 在酷狗音乐中搜索歌手
2. 进入歌手详情页
3. 查看URL中的歌手ID参数
4. 或者通过项目的其他页面（如歌手详情）查看

## 技术实现
- 前端：Vue 3 + Vite
- API调用：Axios
- 下载方式：浏览器原生下载（Blob）
- 路由：Vue Router（Hash模式）
